# Backend Requests — Запросы к бекенд-команде

> **Дата создания**: 2026-02-16
> **Связанные документы**: [BACKEND_API_DOCS.md](./BACKEND_API_DOCS.md) · [INTEGRATION_PLAN.md](./INTEGRATION_PLAN.md)
>
> Здесь фиксируются все расхождения между текущим бекенд API и потребностями фронтенда.
> Фронтенд разрабатывается **так, будто бекенд уже возвращает** эти данные.
> Статусы: `⏳ pending` · `✅ done` · `❌ rejected` · `🔄 in discussion`

---

## 1. Расширение ролей пользователей

**Фаза**: 6 · **Приоритет**: высокий

Текущий бекенд поддерживает `role: "user" | "admin"`.
Фронтенд ожидает расширенный набор ролей:

```typescript
role: "user" | "agent" | "agency" | "developer" | "admin"
```

- `agent` — агент недвижимости (физлицо)
- `agency` — агентство недвижимости (организация)
- `developer` — застройщик

Каждая роль влияет на:
- Видимость UI-элементов (панель агента, управление объектами)
- Доступные действия (публикация объектов, доступ к статистике)
- Тарифные планы

**Статус**: ⏳ pending

---

## 2. Объединение Search Tabs и Saved Filters

**Фаза**: 5 · **Приоритет**: высокий

На фронтенде каждый **таб поиска** содержит в себе **фильтры** — это единая сущность.
Текущий бекенд разделяет:
- `/api/v1/search-tabs` — вкладки (UI-состояние + ссылка на фильтр)
- `/api/v1/filters` — сохранённые фильтры (параметры поиска)

**Запрос**: объединить в один ресурс или обеспечить автоматическую связь:
- При создании таба → автоматически создаётся фильтр
- При обновлении таба → обновляется связанный фильтр
- `TabResponse` должен включать `filters` inline (не только `filter_id`)
- При удалении таба → удаляется связанный фильтр

Альтернатива: фронтенд может делать 2 запроса, но это усложняет логику и увеличивает латентность.

**Статус**: ✅ done (бекенд добавил `filterId` в `TabResponse`, фронтенд использует `filters` inline + `filterId` для связи)

---

## 3. Autosearch: режим "online" (realtime)

**Фаза**: 6 · **Приоритет**: средний

Текущий бекенд поддерживает `send_frequency: "instant" | "daily" | "weekly"`.
Фронтенд ожидает дополнительный режим:

```typescript
send_frequency: "online" | "instant" | "daily" | "weekly"
```

**Режим `online`**:
- Если пользователь онлайн и у него активирован автоподбор → держим WebSocket-соединение
- Бекенд пушит новые подходящие объекты через WS-канал `autosearch:user_{userId}` в реальном времени
- Фронтенд отображает уведомление + добавляет объект в ленту

**Если пользователь offline**:
- Переключение на push-уведомления (mobile) или telegram

**Статус**: ⏳ pending

---

## 4. Группировка атрибутов объекта по секциям

**Фаза**: 2 · **Приоритет**: средний

Фронтенд рендерит атрибуты в разных секциях:
- **Характеристики** (`characteristics`) — площадь, этаж, комнаты
- **Удобства** (`amenities`) — бассейн, парковка, кондиционер
- **Информация о здании** (`estate_info`) — год постройки, тип здания
- **Энергоэффективность** (`energy_efficiency`) — класс, сертификат
- **Арендаторы** (`tenants`) — текущие арендаторы
- **Требования к арендаторам** — минимальный доход, депозит

**Вопрос**: Бекенд уже группирует атрибуты по секциям (отдельные поля `characteristics`, `estate_info`, `energy_efficiency`, `tenants` в `PropertyDetails`)?

Если да — отлично, фронтенд будет рендерить каждую секцию отдельно.
Если нет — нужно либо:
- Добавить поле `section: string` в `Attribute` (`{ label, value, icon_type, section }`)
- Или вернуть атрибуты как отдельные массивы (текущая доковская структура)

**Статус**: 🔄 in discussion

---

## 5. Геометрии для неавторизованных пользователей

**Фаза**: 5 · **Приоритет**: средний

Для неавторизованных пользователей фронтенд использует `filter_id = "0"` (или специальный "guest" ID)
для временного хранения геометрий (полигоны, изохроны, радиусы).

**Запрос**:
- Поддержать `POST /api/v1/filters/0/geometry` без авторизации
- Или альтернативный эндпоинт для guest-геометрий
- Периодическая очистка неиспользуемых guest-геометрий (TTL / cron)

Альтернатива: неавторизованные используют `geojson` inline-параметр (без сохранения). Но это менее удобно для сложных геометрий.

**Статус**: ✅ done (бекенд добавил `/filters/guest/geometry` эндпоинты, фронтенд интегрирован)

---

## 6. Favorites: недостающие поля

**Фаза**: 6 · **Приоритет**: низкий

### PropertyNote

Фронтенд ожидает / использует:
- `content: string` (в доке ✅)
- `tags: string[]` (в доке ✅)
- `property_id: string` — required (в доке ✅)
- `reminder_at: string` (ISO 8601) — в доке ✅
- `reminder_message: string` — в доке ✅

Всё ОК, фронтенд будет следовать доке.

### FavoriteProfessional

Текущая дока описывает `contact_id` + `professional_type`.
Фронтенд хочет также получать:
- Данные профессионала inline (имя, аватар, компания) — чтобы не делать отдельный запрос
- Или эндпоинт `/contacts/:id` для получения деталей

**Запрос**: Добавить inline-данные профессионала в `FavoriteProfessionalResponse`:
```typescript
interface FavoriteProfessionalResponse {
  id: string;
  contact_id: string;
  professional_type: "agent" | "agency";
  // --- Запрашиваемые поля ---
  name: string;
  avatar_url?: string;
  company_name?: string;
  phone?: string;
  // --------------------------
  contact_requested: boolean;
  contact_requested_at?: string;
  messages_count: number;
  notes?: string;
  created_at: string;
  updated_at: string;
}
```

**Статус**: ⏳ pending

---

## 7. Кластеры на карте: property_ids в feature properties

**Фаза**: 4 · **Приоритет**: высокий

При клике на кластер (zoom ≤ 15) фронтенд ожидает получить список `property_ids` из feature properties тайла, чтобы запросить объекты через `include_ids`.

**Вопрос**: Содержат ли кластерные features в MVT тайлах поле `property_ids` (или `cluster_id` для подзапроса)?

Если нет — нужно добавить:
- Для малых кластеров (≤ 50 объектов): `property_ids: string[]` в feature properties
- Для больших кластеров (> 50): `cluster_id` → отдельный эндпоинт `GET /api/v1/properties/cluster/:cluster_id/items`

**Статус**: 🔄 in discussion

---

## 8. Marker type "view" / "no_view" в фильтрах

**Фаза**: 6 · **Приоритет**: низкий

Фронтенд хочет фильтровать по просмотренности:
- Показать только просмотренные
- Показать только непросмотренные

Текущий бекенд не имеет фильтра по views в listing endpoints.

**Запрос**: Добавить query params:
- `viewed=true` — только просмотренные авторизованным пользователем
- `viewed=false` — только непросмотренные

Или использовать `exclude_marker_types` / `include_marker_types` для этого.

**Статус**: ⏳ pending

---

## Changelog

| Дата | Описание |
|------|----------|
| 2026-02-16 | Первичное заполнение при составлении Integration Plan |
| 2026-02-17 | Обновлены статусы #2 (Tabs+Filters) и #5 (Guest-геометрии) → ✅ done |
