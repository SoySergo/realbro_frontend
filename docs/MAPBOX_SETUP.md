# 🗺️ Настройка Mapbox

## Получение токена

1. Перейдите на [https://account.mapbox.com/](https://account.mapbox.com/)
2. Зарегистрируйтесь или войдите в аккаунт
3. Перейдите в раздел **Access tokens**
4. Скопируйте **Default public token** или создайте новый

## Настройка проекта

1. Откройте файл `.env.local` в корне проекта
2. Замените значение `NEXT_PUBLIC_MAPBOX_TOKEN`:

```bash
NEXT_PUBLIC_MAPBOX_TOKEN=pk.your_actual_token_here
```

3. Перезапустите dev сервер:

```bash
pnpm dev
```

## Проверка работы

1. Откройте страницу `/search` в браузере
2. Вы должны увидеть интерактивную карту с маркерами объектов недвижимости
3. Попробуйте:
   - Перемещаться по карте (drag)
   - Зумировать (колесико мыши или кнопки +/-)
   - Кликать по маркерам

## Возможные проблемы

### Карта не отображается
- Проверьте, что токен правильно скопирован в `.env.local`
- Убедитесь, что токен начинается с `pk.`
- Перезапустите dev сервер после изменения `.env.local`

### Ошибка "Invalid token"
- Токен неверный или истек
- Создайте новый токен на Mapbox

### Карта серая/пустая
- Проверьте интернет-соединение
- Убедитесь, что у токена есть доступ к выбранному стилю карты
