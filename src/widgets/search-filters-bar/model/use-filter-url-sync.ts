'use client';

/**
 * @deprecated URL sync is now handled by nuqs in useFilters().
 * This hook is a no-op kept for backward compatibility.
 */
export function useFilterUrlSync() {
    // No-op: nuqs handles URL sync automatically
}
